import { Outlet, Link } from "react-router-dom";

export default function Signup() {
    return (
      <main>
            <input type="text" />
            <Link to="/">SignUp</Link>
      </main>
    )
  }